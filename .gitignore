node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi

# Cloudflare Workers generated types — regenerate with `pnpm cf-typegen`
worker-configuration.d.ts
__unconfig*
todos.json

# MCP server config (local-only)
.mcp.json

# Supabase CLI local state
supabase/.temp/

# design-sync (claude.ai/design) — build artifacts, transient scratch, verification
# working state, staged converter scripts. See .design-sync/ for the committed durable set.
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/

.dev.vars*
!.dev.vars.example
!.env.example
