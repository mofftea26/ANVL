# Prompt 06 — Build Drop Editor shell with save workflow

> **DEPRECATED (2026-06) � do not run.** Drop-builder CMS removed. See `docs/prompts/README.md` and `docs/landing-pages.md`.

```txt
Before coding, read AGENTS.md, docs/features/drops-cms.md, docs/features/acts-builder.md, docs/design-system.md.

Task: Build the Drop Editor shell.

Editor sections:
1. Basic Info
2. Theme & Branding
3. Acts Builder placeholder
4. Products Assignment placeholder
5. SEO placeholder
6. Save/Publish controls

Requirements:
- Live draft state should not mutate published drop until Save.
- Save button should show confirmation/success state.
- Include checkbox/toggle: “Make this drop active after saving”.
- Include schedule option with date/time.
- Include preview panel placeholder.
- Validate required fields: title, slug, theme palette.
- Keep UI simple and spacious.

Do not build the full acts builder yet.
Update docs/changelog.md.
```
