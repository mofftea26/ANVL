# Prompt 10 — Build active Drop page and dynamic nav label

```txt
Before coding, read AGENTS.md, docs/features/drops-cms.md, docs/features/products-commerce.md, docs/design-system.md, docs/features/seo.md.

Task: Refactor the Drop page so the top-bar Drop link is based on the active drop.

Requirements:
- Header nav label should use active drop title, e.g. “The Oath”.
- Drop page should show title, subtitle, background media/emblem, description, release date/countdown if relevant, and assigned product cards.
- Use active drop theme.
- Product cards link to product details.
- SEO metadata comes from drop SEO document.
- SSR-safe route loader.
- Mobile fast, desktop premium visuals.

Update docs/changelog.md.
```
