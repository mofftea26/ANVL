/** Strip scripts and coerce fills/strokes to `currentColor` for theme tinting. */
export declare function themeSvgMarkupForTint(svg: string): string;
export declare function isSvgEmblemUrl(url: string): boolean;
