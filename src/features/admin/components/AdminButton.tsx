/**
 * Admin feature entrypoint for the shared pressable control.
 * Styling uses oath-dark tokens (`--color-*` in `src/styles.css`) and `focus-ring`.
 */
export {
  Button as AdminButton,
  buttonVariants as adminButtonVariants,
  type ButtonProps as AdminButtonProps,
} from '@/shared/components/ui/Button'
