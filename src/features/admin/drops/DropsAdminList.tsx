/**
 * Placeholder — the admin drops route imports this module on `origin/cms` before the full list ships.
 * Replace with the real TanStack Query–backed list when drop management is complete.
 */
export function DropsAdminList() {
  return (
    <p className="text-sm text-[var(--color-text-muted)]">
      Drops list UI is not bundled in this branch snapshot. Use per-drop routes from the admin
      navigation when available.
    </p>
  )
}
