# Local CMS admin gate (Vite — values are bundled into the client for dev/demo only).
# Use a strong secret locally; replace with real server auth before production.
VITE_ANVL_ADMIN_USERNAME=admin
VITE_ANVL_ADMIN_PASSWORD=
